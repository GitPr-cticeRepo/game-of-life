pipeline {
    agent { label 'LINUX' }
    stages {
        stage('source') {
            steps {
                git 'https://github.com/GitPr-cticeRepo/game-of-life.git'
            }
        }
        stage('package') {
            steps {
                sh 'mvn package'
            }
        }
    }
    
}
